# ============================================================
# Scene Template: Master WC
# ============================================================
# HOW TO USE:
# 1. Copy this entire file content
# 2. Paste into scenes.yaml
# 3. Change 'name' to your desired scene name (keep prefix)
# 4. Change 'icon' to match the scene purpose
# 5. Set each entity's 'state' to 'on' or 'off'
# 6. For dimmable lights, add 'brightness: 0-255'
# 7. Restart Home Assistant or reload scenes
#
# Example brightness levels:
#   brightness: 255  # Full brightness
#   brightness: 128  # 50% brightness
#   brightness: 64   # 25% brightness
# ============================================================

- id: '1767893225711'
  name: 'mwc_new_scene'
  icon: 'mdi:lightbulb-group'
  entities:
    light.mwc_ceiling_shower:
      state: 'on'
    light.mwc_ceiling_toilet:
      state: 'on'
    light.mwc_lt_bathroom_wall_e:
      state: 'on'
    light.mwc_lt_bathroom_wall_s:
      state: 'on'
    light.mwc_main_ceiling:
      state: 'on'
    light.mwc_main_wall:
      state: 'on'
  metadata:
    light.mwc_ceiling_shower:
      entity_only: true
    light.mwc_ceiling_toilet:
      entity_only: true
    light.mwc_lt_bathroom_wall_e:
      entity_only: true
    light.mwc_lt_bathroom_wall_s:
      entity_only: true
    light.mwc_main_ceiling:
      entity_only: true
    light.mwc_main_wall:
      entity_only: true
