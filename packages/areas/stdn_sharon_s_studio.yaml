# Auto-generated by hass-data-generator
# Area: Sharon's Studio (stdn_)
# Do not edit manually - changes will be overwritten

group:
  stdn_lights:
    name: Sharon's Studio Lights
    entities:
      - light.stdn_lt_wall_e
      - light.stdn_lt_wall_n
      - light.stdn_lt_wall_w

input_select:
  stdn_active_scene:
    name: Sharon's Studio Active Scene
    options:
      - "none"
      - scene.stdn_standard
      - scene.stdn_minimal
    initial: "none"

input_datetime:
  stdn_last_presence:
    name: Sharon's Studio Last Presence
    has_date: true
    has_time: true

input_boolean:
  stdn_pause_automations:
    name: Sharon's Studio Pause Automations
    icon: "mdi:pause-circle"

timer:
  stdn_vacancy:
    name: Sharon's Studio Vacancy Timer
    duration: "00:09:00"

template:
  - sensor:
      - name: Sharon's Studio People Present
        unique_id: stdn_people_present
        state: "{{ states.person | selectattr('state', 'eq', 'sharon_s_studio') | map(attribute='name') | list | join(', ') or 'none' }}"
