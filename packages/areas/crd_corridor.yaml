# Auto-generated by hass-data-generator
# Area: Corridor (crd_)
# Do not edit manually - changes will be overwritten

group:
  crd_lights:
    name: Corridor Lights
    entities:
      - light.crd_lt_mmd_wall_n
      - light.crd_lt_wall_e

input_select:
  crd_active_scene:
    name: Corridor Active Scene
    options:
      - "none"
    initial: "none"

input_datetime:
  crd_last_presence:
    name: Corridor Last Presence
    has_date: true
    has_time: true

input_boolean:
  crd_pause_automations:
    name: Corridor Pause Automations
    icon: "mdi:pause-circle"

timer:
  crd_vacancy:
    name: Corridor Vacancy Timer
    duration: "00:09:00"

template:
  - sensor:
      - name: Corridor People Present
        unique_id: crd_people_present
        state: "{{ states.person | selectattr('state', 'eq', 'corridor') | map(attribute='name') | list | join(', ') or 'none' }}"
