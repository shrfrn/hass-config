# Auto-generated by hass-data-generator
# Area: Home Assistant (hacs_)
# Do not edit manually - changes will be overwritten

input_select:
  hacs_active_scene:
    name: Home Assistant Active Scene
    options:
      - "none"
    initial: "none"

input_datetime:
  hacs_last_presence:
    name: Home Assistant Last Presence
    has_date: true
    has_time: true

input_boolean:
  hacs_pause_automations:
    name: Home Assistant Pause Automations
    icon: "mdi:pause-circle"

timer:
  hacs_vacancy:
    name: Home Assistant Vacancy Timer
    duration: "00:9:00"

template:
  - sensor:
      - name: Home Assistant People Present
        unique_id: hacs_people_present
        state: "{{ states.person | selectattr('state', 'eq', 'home_assistant') | map(attribute='name') | list | join(', ') or 'none' }}"
